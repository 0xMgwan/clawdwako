[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.openclaw"

[deploy]
startCommand = "openclaw start --gateway --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
