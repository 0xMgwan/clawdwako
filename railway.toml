[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.openclaw"

[deploy]
startCommand = "openclaw gateway --allow-unconfigured --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
